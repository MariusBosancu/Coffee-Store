/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, //reach18 -> in airtable apare item ul dublicat daca e true
};
module.exports = {
  images: {
    domains: ["images.unsplash.com"],
  },
};

//store variables (old version->new version .env.local)
